version: '3'

services:
  heurly:
    container_name: beta.heurly
    image: domesday/heurly:beta
    labels:
      logging: "promtail"
      logging_jobname: "containerlogs"
    networks:
      - heurly-network
    env_file:
      - .env

networks: 
  heurly-network: 
    external: true